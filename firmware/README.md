# Firmware & Updates

This folder contains official CyberFlipper firmware, update files, and binaries for Flipper Zero.

- Place `firmware.dfu`, `updater.bin`, `update.fuf`, etc. here.
- See `docs/FIRMWARE_UPDATE.md` for instructions.
