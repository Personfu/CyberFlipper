# Web Dashboard

This folder contains the CyberFlipper web dashboard and all static site files (HTML, CSS, JS, images, assets).

- Place `index.html`, `about.html`, `stats.html`, etc. here.
- Place all dashboard assets in `assets/` and `images/`.
- See `docs/BRANDING.md` for theming guidelines.